#include "main.h"

/**
 * _islower - checks
 * @c: is char to be ckecked
 * Return: 1 if char is lowercase, otherwise 0.
 */

int _slower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
